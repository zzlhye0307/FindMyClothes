//
//  DataController.swift
//  Project
//
//  Created by mac on 06/08/2019.
//  Copyright © 2019 mac. All rights reserved.
//

import UIKit
import CoreData

class DataController: NSObject {
/*
    func insertItemToFavorite(item: Favorite) {
        guard let appDelegate = UIApplication.shared.delegate as? AppDelegate else {
            return
        }
        let managedContext = appDelegate.persistentContainer.viewContext
        let favoriteEntity = NSEntityDescription.entity(forEntityName: "Favorite", in: managedContext)!
        let favoriteItem = NSManagedObject(entity: favoriteEntity, insertInto: managedContext)
        favoriteItem.setValue(item.id, forKeyPath: "id")
        favoriteItem.setValue(item.title, forKey: "title")
        favoriteItem.setValue(item.price, forKey: "price")
        favoriteItem.setValue(item.link, forKey: "link")
        favoriteItem.setValue(item.img, forKey: "img")
        
        do {
            try managedContext.save()
        } catch let error as NSError {
            print("error : \(error)")
        }
    }
 */
    
//    func deleteItemAtFavorite()
}
